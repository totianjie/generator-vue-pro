import VCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
VCheckboxButton.install = function (Vue) {
    Vue.component(VCheckboxButton.name, VCheckboxButton);
};

export default VCheckboxButton;
