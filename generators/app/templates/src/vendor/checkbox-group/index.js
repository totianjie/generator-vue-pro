import VCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
VCheckboxGroup.install = function (Vue) {
    Vue.component(VCheckboxGroup.name, VCheckboxGroup);
};

export default VCheckboxGroup;
